import 'package:flutter/material.dart';

/// Name of the app
const String appName = "Expense";

// colors
final Color scaffoldColor = Color(0xFF030507);

/// default Animation duration
const Duration defaultAnimationDuration = Duration(milliseconds: 1100);
